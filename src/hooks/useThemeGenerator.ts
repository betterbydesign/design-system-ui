/**
 * useThemeGenerator Hook
 * 
 * Re-exports the useThemeGenerator hook from the context for convenience.
 * This file exists to maintain the planned file structure.
 */

export { 
  useThemeGenerator,
  type ThemeGeneratorState,
  type ColorConfig,
  type PreviewMode,
  type PreviewDevice,
} from '@/contexts/ThemeGeneratorContext';
